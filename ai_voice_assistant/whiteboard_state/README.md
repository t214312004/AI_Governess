# Whiteboard State

Runtime whiteboard state and generated display assets live here.

Only this placeholder file should be committed. `active.json`, `.whiteboard.lock`,
and `assets/` are local runtime state and must stay untracked.
